# Create breaks for Resonance plots

Create breaks for Resonance plots

## Usage

``` r
plotDC_helper(useTimeVector, timeStamp, win, df_DC, trimFirstWin = TRUE)
```

## Arguments

- useTimeVector:

  useTimeVector

- df_DC:

  df_DC

## Value

breaks
