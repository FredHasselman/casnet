# Add transparency to a colour

Add transparency to a colour

## Usage

``` r
add_alpha(col, alpha = 1)
```

## Arguments

- col:

  A colour name, hexadecimal string or positive integer `i`, such that
  `palette()[i]`

- alpha:

  Alpha transparency value

## Value

An rgb colour with transparency
